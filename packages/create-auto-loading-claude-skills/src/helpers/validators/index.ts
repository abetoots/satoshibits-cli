/**
 * Pre-built validators
 *
 * Production-ready, configurable validators for common patterns
 */

import { layeredArchitecture } from './layered-architecture.js';

export const validators = {
  layeredArchitecture
};
