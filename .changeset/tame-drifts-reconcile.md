---
"@satoshibits/doc-lint": minor
---

adds code-aware support: a reconcile mode with a documentation↔code drift scanner, a code-first bootstrap on-ramp (deterministic as-built scaffolds plus a documentation gap inventory), a `scan` command backed by a static code map, and new concerns including code-vs-doc parity
